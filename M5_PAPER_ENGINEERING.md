# M5 论文工程
## Living Paper Finalization：不是从零写，而是把 M1–M4 已积累证据重构成正式论文

论文的职责是把研究证据按评委阅读顺序重建出来，而不是把项目日志复制进去。

---

# 5.0 Living Paper 工作机制

M5 不是论文第一次出现的地方。

在前面阶段应已持续形成：
- M1：问题分析、Data Findings、假设、Pitfall说明草稿；
- M2：模型动机、变量/公式、Why-not、Bound；
- M3：算法职责、优化历史；
- M4：验证、Gap、消融、敏感性、Case Study。

M5 的任务是：
1. 删除项目日志式语言；
2. 按评委阅读顺序重新组织；
3. 压缩重复；
4. 补逻辑过渡；
5. 统一术语/符号/数字；
6. 将证据变成完整论证。

“即做即写”指同步沉淀证据草稿，不代表未经验证的中间结论直接进入终稿。

# 5.1 Paper Architecture

写前先建立：

| Section | Purpose | Reader leaves knowing | Claim | Evidence | Figure/Table | Length |
|---|---|---|---|---|---|---|

---

# 5.2 摘要

最后定稿。

总述：
1. 问题；
2. 真正难点；
3. 总框架；
4. 多目标/总体原则。

每问：
1. 本问结构；
2. 模型决定什么；
3. 目标/关键约束；
4. 具体算法及职责；
5. 主结果；
6. 最强可信度证据。

数字不加粗；核心具体算法/框架可加粗。

---

# 5.2A Information-rich Section Titles

章节标题应尽量传递内容，而不是只写：
- “问题一解答”；
- “模型一”；
- “结果分析”。

推荐：
> “基于动态容量取送模型的联合运输优化”

> “基于滚动时序验证的关注度预测”

但标题不要堆叠五六个算法缩写。标题负责告诉评委“这一节解决什么”，正文再解释怎么做。

# 5.3 问题重述

只写：
> 条件 → 决策/预测 → 目标 → 输出。

不提前写算法。

---

# 5.4 问题分析必须来自 M1，而不是事后编故事

每问建议 5 段：

## 第1段：数学本质
本质是什么。

## 第2段：真正难点与坑点
不是罗列“复杂”，而要说明：
> 错误解释/简单处理会导致什么后果。

## 第3段：数据和题面揭示的可利用结构
引用 Data Findings：
> 哪些规律允许聚合、分层、时序化、候选化。

## 第4段：模型选择
> 为什么当前模型比常见替代更合适。

## 第5段：求解路线
自然过渡到 Formal Model。

禁止：
> 先确定算法，再回头找理由。

---

# 5.5 数据理解与预处理

这一节不应只写“去缺失、标准化”。

推荐：

## 5.5.1 数据来源与结构
文件、表、实体、关系。

## 5.5.2 数据质量与规则统一
缺失、异常、单位、时间、ID。

## 5.5.3 关键数据结构
只写真正影响建模的 Findings。

每个 Finding：
1. 观察；
2. 图/表证据；
3. 对建模的后果。

## 5.5.4 数据限制
目标未观测、样本小、混杂、信息可用性等。

---

# 5.6 模型假设

每条：
1. 假设；
2. 为什么需要；
3. 依据；
4. 如果不成立会怎样；
5. 后文怎么验证/敏感。

不要写无关常识。

---

# 5.7 模型准备

这是高质量论文的重要过渡。

逐段写：
1. 为什么直接建模不可行/不合理；
2. 数据/状态如何重新表示；
3. 固化什么；
4. 主问题还决定什么；
5. 如何恢复最终输出。

---

# 5.8 变量、目标、约束

每个关键公式：

\[
文字引入 \rightarrow 公式 \rightarrow 新符号 \rightarrow 现实含义
\]

约束按现实规则分组，不连续堆公式。

---

# 5.9 算法

第一段必须：
> 为什么 Formal Model 不能直接用更简单方式求？

然后说明：
- 总架构；
- 各算法职责；
- 针对本题改进；
- 停止；
- 规模。

具体算法可加粗。

---

# 5.10 Why-not-Alternative

对容易被评委追问的方法选择，主动回答：
> 为什么不用 ANOVA / GA / random K-fold / 纯MILP / 连续优化等？

基于数据和结构回答。

---

# 5.11 结果八段协议

1. 直接回答题目；
2. 核心结果表；
3. Baseline；
4. 改善机制；
5. Bound/Gap；
6. 解结构与图；
7. 独立验证；
8. Trade-off / 边界。

---

# 5.12 验证与实验

不要在正文最后凑一节敏感性。

实验应回答：
- 模型有没有错；
- 改进有没有用；
- 算法稳不稳；
- 假设改变会怎样；
- 结论能否泛化。

---

# 5.13 论文中的 Pitfall 怎么呈现

Pitfall Ledger 是内部工程文件，不要整表搬进正文。

只保留真正决定模型的 P0/P1：
> “如果按X理解会导致Y，因此本文采用Z口径。”

这样既体现严谨，又不让论文变成规则检查清单。

---

# 5.14 论文中的 EDA 怎么呈现

不是展示所有探索过程。

只保留：
> 对模型选择/结果解释有证据作用的图。

删除它如果不削弱任何 Claim，就应删。

---

# 5.14A Local Case Study 在论文中的写法

如果 M4 的 Micro Case Study 能显著降低理解成本，可以进入正文：

图/表前：
> “为直观说明约束（x）的作用，选取……构造局部案例。”

正文按：
1. 局部输入；
2. 关键变量/状态；
3. 公式作用；
4. 得到的局部结果；
5. 它与全局模型之间的对应关系。

Case Study 不是用个例替代全局验证，而是让复杂机制可解释。

# 5.15 模型评价

优点：
> 机制 + 证据 + 效果。

局限：
> 来源 + 影响 + 不影响什么。

改进：
> 与局限一一对应。

---

# 5.16 结论

逐问给：
- 核心答案；
- 最强证据；
- 边界。

最后给全题机制认识。

不引入新模型/新实验。

---

# 5.16A 写作顺序与成文顺序分离

建议实际写作顺序：
1. 问题分析素材；
2. 模型与算法；
3. 结果与验证；
4. 数据结构；
5. 评价/结论；
6. 问题重述；
7. 摘要与标题最后定稿。

正式 PDF 顺序仍按当届官方规范和论文逻辑组织。

摘要可以提前积累 Evidence Sheet，但最终数字与结论必须在模型冻结后再定稿。

# 5.17 First Impression Gate

第1页：
> 问题、方法、结果、可信度。

第3页前：
> 三问递进、主要难点、数据/结构认识、总技术路线。

如果第3页还主要在讲背景，重写。


---

# 5.18 2026正文页数责任边界【V4.3.2】

30页限制按以下内容合计：

```text
正文主体
+ AI工具使用声明
+ 参考文献
<= 30页
```

不把摘要计入正文30页。

附录按全国规范不计入30页正文限制。

论文篇幅目标：
> 尽量充分，但绝不为接近30页而增加无证据内容。

---

# 5.19 数学符号写作规范【V4.3.2】

写公式前先建立 Symbol Contract：

```text
Symbol:
Meaning:
Type:
Unit:
Domain:
First occurrence:
Discipline convention:
```

要求：
- 重要符号符合对应领域惯例；
- 单个公式尽量避免长英文单词作为变量或上下标；
- 下标只保留区分含义所必需的信息；
- 同一概念全文只保留一套符号；
- 若论文借鉴经典模型符号，应保持与引用文献基本一致或明确说明变换。

例如：

推荐：
\[
x_{ij},\quad t_i,\quad C_k,\quad q_i,\quad R
\]

不推荐：
\[
numberOfPeople_{facilityDestination}
\]

程序代码中的描述性变量名不受此数学排版规则限制。

---

# 5.20 附录代码的写作边界【V4.3.2】

正文：
- 可用算法伪代码；
- 可展示少量核心代码片段；
- 不用塞大量实现细节。

论文附录：
- 按2026全国格式规范，包含全部完整、可运行的源程序；
- 可按文件分小节排版；
- 每个文件注明用途。

支撑材料：
- 同时提供原始可运行源文件。

因此：
> “正文只展示核心逻辑”与“附录完整代码”同时成立。


---

# 5.21 湖南赛区正文与附录页数策略【V4.3.3】

## 正文

目标：
> 29–30页。

硬上限：
> 30页。

30页包含：
- 正文主体；
- AI 工具使用声明；
- 参考文献。

摘要另1页，不计入正文30页。

写作时使用 `Page Budget Ledger`：

```text
Abstract: 1
Problem + Analysis:
Data:
Q1:
Q2:
Q3:
Validation/Experiments:
Conclusion:
AI Declaration:
References:
Total body:
Appendix:
```

如果正文少于目标很多：
> 优先补充证据链、模型解释、实验、结果机制；
> 不补空话。

## 附录

硬控制：
> <=20页。

必须同时满足：
> 全部最终完整可运行源程序均出现在附录。

因此代码工程要主动控制冗余，而不是最后删核心代码。
